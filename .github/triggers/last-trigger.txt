2026-06-16T13:08Z — WP pages for Maia + Le Méridien now exist (create-wp-page #507 succeeded; bot-gate had cooled). sync-wp raced ahead of page creation on the prior trigger, so the pages are still empty shells. Re-fire sync-wp to push the PDP HTML into raw_html_code, and sync-notion to keep content current — sync-notion
2026-06-16T13:55Z — backfill the new 🖼️ Image 5 (and any still-empty gallery slot) across ALL Live listings, using the new --fill-galleries mode (PR #383) — sync-images:fill-galleries
2026-06-16T14:05Z — re-fire the 🖼️ Image 5 backfill now that --fill-galleries actually reaches already-imaged rows (PR #384 fixed the placeholder-only bulk filter that skipped them; it now sweeps every Live row that has an empty gallery slot AND a primary 📷/Drive/Berkeley source) — sync-images:fill-galleries
2026-07-08T14:20Z — Property ID was never synced (unique_id shape unread) — PR #388 fixed the reader; fire sync-notion to rewrite the stale NAC-4 into real NAC-<n> across all 115 affected PDPs — sync-notion
2026-07-08T14:58Z — re-fire sync-wp: run 952 checked out main BEFORE the sync-notion ID-rewrite commit (GITHUB_TOKEN commits don't chain-trigger workflows), so it pushed the pre-fix HTML; this trigger runs sync-wp against the rewritten files — sync-wp
2026-07-14T15:20Z — AU CLP atlas fixed (PR #393): metro-level City grouping + geographically correct Sydney/Melbourne pins; clp-sync regenerated country/*.html in f8304d6 but that bot commit can't chain-trigger sync-wp-clp and its cron is throttled — fire sync-wp-clp to push the corrected pages live — sync-wp-clp
