2026-06-16T13:08Z — WP pages for Maia + Le Méridien now exist (create-wp-page #507 succeeded; bot-gate had cooled). sync-wp raced ahead of page creation on the prior trigger, so the pages are still empty shells. Re-fire sync-wp to push the PDP HTML into raw_html_code, and sync-notion to keep content current — sync-notion
2026-06-16T13:55Z — backfill the new 🖼️ Image 5 (and any still-empty gallery slot) across ALL Live listings, using the new --fill-galleries mode (PR #383): re-picks from each row's existing source but uploads/writes ONLY blank gallery slots — hero + Image 1-4 are never touched, so nothing can be degraded — sync-images:fill-galleries
